# iam-bindings

Applies additive IAM member bindings at organization, folder, and project scope.

This module intentionally uses `*_iam_member` resources to avoid taking authoritative ownership of complete IAM policies.
